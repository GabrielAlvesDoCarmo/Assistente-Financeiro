<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AssistenteFinanceiro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.AssistenteFinanceiro" parent="Base.Theme.AssistenteFinanceiro" />


    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="splashScreenIconSize">20dp</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_assistent</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.AssistenteFinanceiro</item>
    </style>
</resources>